
Let me gather a few more critical pieces to fully map the call chain.
现在我对整个机制有了清晰的全貌，下面从三个维度详细说明。

---

## 全局大纲关键要素提取机制及其在正文写作中的调用方式

### 一、核心架构总览

整个系统分为**两大阶段**和**一条贯穿始终的状态追踪链路**：

```
阶段一：创意生成（大纲侧）                    阶段二：正文写作（写作侧）
┌──────────────────────────────┐          ┌──────────────────────────────┐
│ GlobalOutlineMixin           │          │ ContextBuilderMixin          │
│   ↓ 生成全局大纲文本          │          │   ↓ _build_context()         │
│ ChapterBoundaryMixin         │          │ UnitDirectMixin              │
│   ↓ 程序化提取章节边界        │          │   ↓ _process_unit_direct()   │
│ AtomicChapterGeneratorMixin  │          │ WriterAgent → _prompts.py    │
│   ↓ 逐章原子化生成单元概述     │          │   ↓ _build_direct_writer_    │
│                               │          │       user_prompt()         │
└──────────────────────────────┘          └──────────────────────────────┘
         ↕                                    ↕
┌──────────────────────────────────────────────────────────────────┐
│  CharacterStateTracker (人物状态追踪器) — 贯穿全程                  │
│  NovelKnowledgeGraph (知识图谱) — 持久化存储                       │
└──────────────────────────────────────────────────────────────────┘
```

---

### 二、关键要素提取机制

#### 2.1 章节边界提取（ChapterBoundaryMixin）

核心文件：[chapter_boundary.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/services/outline_generator/impl/mixins/chapter_boundary.py)

这是整个系统最关键的**程序化提取**环节。`extract_chapter_boundaries()` 方法实现了一套三级提取策略：

**第一级：结构化定位**（第 46-53 行）
```python
_CHAPTER_OUTLINE_HEADER_RE = re.compile(
    r'#+\s*(?:单元内容规划|分章大纲|章节分配|章节划分|分章规划|章节计划)'
)
```
先在全局大纲中定位「单元内容规划」或「分章大纲」段落。这一段的格式是 LLM 在生成全局大纲时被要求输出的结构化段落。

**第二级：按行解析**（第 97-139 行）
- **范围行匹配**：`第11-30章：江湖历练` → 为第 11~30 章每章分配"江湖历练"边界描述
- **单章行匹配**：`- 第1章：主角穿越` → 为第 1 章精确分配边界
- 使用中文数字→阿拉伯数字转换（`_parse_chapter_number`），兼容"第十一章"和"第11章"

**第三级：推断回退**（第 140-145 行）
```python
for ch_num in range(1, unit_count + 1):
    if ch_num not in boundary_map:
        boundary_map[ch_num] = self._infer_boundary(
            ch_num, boundary_map, unit_count, unit_label)
```
对于未被覆盖的章节，通过相邻章节边界推断或使用范围描述补充。

**输出**：`Dict[int, str]` — 章节号 → 边界描述文本的映射，例如 `{1: "主角穿越到异世界", 2: "初识魔法体系", ...}`

#### 2.2 单元概述内容隔离（AtomicChapterGeneratorMixin）

核心文件：[atomic_chapter_generator.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/services/outline_generator/impl/mixins/atomic_chapter_generator.py)

`_extract_chapter_outline_segment()` 方法（第 625-709 行）是内容隔离的核心：

```python
# 构造正则：匹配"第X章"及其专属内容段落
patterns = [
    rf"第[{chinese_nums}\d]*{chinese_chapter}[{chinese_nums}\d]*章[^\n]*\n(?:(?!第[{chinese_nums}\d]+章).)*",
    rf"第[{chinese_nums}\d]*{chapter_num}[{chinese_nums}\d]*章[^\n]*\n(?:(?!第[{chinese_nums}\d]+章).)*",
    # ... 多层回退模式
]
```

**核心原则**：禁止对 `global_outline` 做字符串切片 `[:N]`，而是通过正则精确提取当前章节对应的大纲段落。使用**负向前瞻** `(?!第...)章` 确保不会跨章节提取。

兼容中文数字（"七"→7）和阿拉伯数字（"7"），且根据 `content_type`（小说/剧集/电影）使用不同的单元标签（章/集/场）。

**v5.0 关键变化**（第 224-272 行）：不再使用 GraphRAG 检索知识图谱来构建上下文，而是**直接传入完整的 `global_outline`**。注释明确说明了原因：
> GraphRAG检索会丢失角色年龄、关系、时间线等细粒度信息，导致AI在生成时出现人物关系混淆（张冠李戴）。

#### 2.3 人物设定提取（_context_builder.py + CharacterStateTracker）

核心文件：[`_context_builder.py`](file:///e:/python_project/全能创意大师（开发版）/backend/app/services/writing_engine/pipeline/_context_builder.py)（第 36-98 行）

采用的是**三级回退策略**：

```
优先级1: 知识图谱 → NovelKnowledgeGraph.get_character_profiles()
    ↓ 失败
优先级2: 大纲结构化字段 → outline["characters"] / ["人物设定"] / ["main_characters"]
    ↓ 失败  
优先级3: CharacterStateTracker.generate_character_profiles_from_outline()
          → LLM从大纲文本中提取并生成完整人物设定
```

`_extract_characters_from_outline()` 方法（第 311-349 行）支持多种 JSON 结构兼容：
- `outline.characters[]` — 标准字段名
- `outline.人物设定[]` — 中文字段名
- `outline.main_characters[]` — 另一种命名约定
- 每个角色支持 `name/role/personality/background/description` 等属性

#### 2.4 知识图谱实体类型体系

核心文件：[graph_rag_config.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/tools/graph_rag_config.py)（第 261-300 行）

定义了完整的实体类型层次：

| 层级 | 实体类型 | 属性 |
|------|---------|------|
| 宏观层 | 主题 | 名称、描述、相关人物、相关事件 |
| 宏观层 | 世界观规则 | 名称、描述、适用范围、影响对象 |
| 宏观层 | **人物** | 姓名、别名、年龄、性别、性格、背景故事、初始目标、外貌描述 |
| 宏观层 | 故事结构 | 结构名称、阶段描述、包含的宏观事件 |
| 宏观层 | 章节概要 | 章节号、标题、概要、宏观冲突、涉及人物、涉及地点 |
| 宏观层 | 地点 | 地点名称、描述、特征 |

---

### 三、正文写作时的调用流程

#### 3.1 上下文构建入口

核心文件：[_context_builder.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/services/writing_engine/pipeline/_context_builder.py) → `_build_context()`（第 36-309 行）

构建的 `AgentContext` 包含以下关键字段，这些是传递给写手 Agent 的全部"知识"：

```python
AgentContext(
    outline=outline,                          # 全局大纲（结构化）
    global_context=global_context,            # 全局上下文文本
    character_profiles=character_profiles,    # 人物设定列表
    world_settings=world_settings,            # 世界观设定
    style_guide=style_guide,                  # 风格指南
    unit_summaries=unit_summaries,            # 所有单元的概述（键值对）
    config={
        "content_type": content_type,
        "generation_mode": "direct",
        "unit_summaries": unit_summaries,     # 单元概述传入 config
        ...
    }
)
```

关键点：`unit_summaries`（单元概述）同时存在于 `context.config` 中，写作时写手Agent通过它获取每章的具体指导。

#### 3.2 逐章写作时的知识注入

核心文件：[_unit_direct.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/agents/writing/orchestrator_agent/content_pipeline/_unit_direct.py) → `_process_unit_direct()`（第 49-220 行）

这是正文写作的**核心调用链路**，每一步都在为写手 Agent 注入更多知识：

```
步骤1: 获取人物状态快照
  → CharacterStateTracker.get_state_for_prompt(chapter_num=unit_index)
  → 返回: 所有人物当前状态、位置、身份、关系的格式化文本

步骤2: 获取知识图谱上下文  
  → CharacterStateTracker.get_knowledge_graph_context_for_writing(chapter_num, max_entities=20)
  → 返回: 前文扩展实体（设施、事件、群体、道具、伏笔、规则）的格式化文本

步骤3: 获取扩展一致性上下文
  → NovelKnowledgeGraph.format_consistency_report_for_prompt(unit_index)
  → 返回: 单元图谱的完整一致性报告

步骤4: 合并为统一上下文
  → full_kg_context = character_state_snapshot + knowledge_graph_context + extended_consistency_context

步骤5: 构建 WriterAgent 上下文
  → writer_context = AgentContext(
        outline=...,                        # 全局大纲
        global_context=...,                 # 全局上下文
        character_profiles=...,             # 人物设定
        unit_summary=unit.unit_summary,     # ✅ 当前单元的概述
        unit_title=unit.unit_title,         # ✅ 当前单元标题
        character_state_snapshot=...,       # ✅ 人物状态快照
        previous_content=...,               # ✅ 前文内容
        extra={"knowledge_graph_context": full_kg_context}  # ✅ 合并后的KG上下文
    )

步骤6: 调用写手Agent
  → result = await writer_agent.execute(writer_context)
```

#### 3.3 写手 Agent 提示词组装

核心文件：[_prompts.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/agents/writing/writer_agent/_prompts.py) → `_build_direct_writer_user_prompt()`（第 71-237 行）

最终传递给 LLM 的提示词结构（按权重从高到低）：

```
【全局故事背景（核心参考）】          ← global_outline 完整文本（v5.0）
  整个故事的全局背景和主线设定

【风格文档特征】                      ← style_document_features（前2000字）
  上传的风格文档分析结果

【本章创作指南（单元概述）】           ← unit_title + unit_summary
  当前章节的具体创作指导

【前文知识图谱参考（架构优化v3.1）】    ← knowledge_graph_context（合并）
  从追踪器和图谱提取的完整知识信息

【人物状态追踪】                      ← character_state_snapshot
  身份、位置、关系、性格变化

【人物关系链】                        ← relationship_summary

【人物当前位置】                      ← character_location_map
  每个角色当前所在地点

【人物身份/官职】                     ← character_identity_map
  每个角色当前的身份/官职

【前文内容（最后3000字）】            ← previous_content[-3000:]
  故事进展

【上一章结尾处（最后500字）】          ← previous_content[-500:]
  精确接续点，LLM由此直接继续

【衔接要求】                          ← 5条硬性衔接规则
  禁止重复、保持一致性等
```

---

### 四、人物状态一致性的跨章节保障机制

这是保证"人物状态、位置、身份在不同章节间连续"的**核心链路**：

#### 4.1 CharacterStateTracker 状态机

核心文件：[character_state_tracker](file:///e:/python_project/全能创意大师（开发版）/backend/app/agents/writing/character_state_tracker/impl/generator.py)（第 40-71 行）

组合了 **28 个 Mixin**，每个负责一个功能域：

| Mixin | 功能 |
|-------|------|
| `InitializeMixin` | 从人物设定初始化追踪器 |
| `GetStateForPromptMixin` | 格式化为写作提示词可用文本 |
| `UpdateCharacterStateMixin` | 每章写完后更新人物状态 |
| `RecordChapterSnapshotMixin` | 记录每章快照（用于回看） |
| `ExportToKnowledgeGraphMixin` | 导出到知识图谱持久化 |
| `SyncUnitToGlobalGraphMixin` | 将单元图谱同步到全局图谱 |
| `GetKnowledgeGraphContextForWritingMixin` | 构建前文知识参考文本 |
| `CheckConsistencyMixin` | 一致性检查 |
| `GetStateEvolutionMixin` | 获取某角色的状态演变历史 |
| `GetRelationshipSummaryMixin` | 获取关系变化摘要 |

#### 4.2 状态追踪数据流

```
写作前（注入上下文）:
  CharacterStateTracker.get_state_for_prompt(chapter_num=N)
    → 汇总所有人物当前: identity, location, personality, relationships
    → 格式化为文本 → 注入 WriterAgent 提示词

写作后（更新状态）:
  LogicEditor 分析新生成内容
    → 提取人物状态变化（身份变化、位置移动、关系变化）
    → CharacterStateInterface.apply_state_updates()
      → CharacterStateTracker.update_character_state()
        → 更新内存状态
        → RecordChapterSnapshotMixin 记录快照
        → ExportToKnowledgeGraphMixin 持久化到知识图谱
```

#### 4.3 知识图谱双轨持久化

```
全局图谱（project.global_outline_graph_path）:
  - 存储: 人物基础设定、世界观规则、故事结构
  - 更新时机: 大纲生成后构建一次，之后增量同步

单元图谱（每章一个 graph 文件）:
  - 存储: 本章人物状态变化、位置变化、关系变化
  - 更新时机: 每章写作完成后同步
  - 合并: SyncUnitToGlobalGraphMixin 定期将单元图谱合并到全局图谱
```

#### 4.4 原子化生成中的边界验证闭环

核心文件：[chapter_boundary.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/services/outline_generator/impl/mixins/chapter_boundary.py) + [chapter_boundary_llm.py](file:///e:/python_project/全能创意大师（开发版）/backend/app/services/outline_generator/impl/mixins/chapter_boundary_llm.py)

采用**两级验证链**：

```
第一级：关键词预筛选 (keyword_prefilter)
  → 提取本章内容关键词
  → 提取后续5章的边界关键词
  → 检测重叠（过滤<4字的通用词）
  → 无重叠 → 直接放行

第二级：LLM语义验证 (只有关键词预筛发现疑似越界时才触发)
  → 给LLM当前章节内容 + 当前边界 + 下2章边界
  → 询问："本章是否包含了后续章节才应出现的内容？"
  → LLM返回 passed/confidence/violations
  → 失败 → 自动重试（最多2次，逐次降低temperature）
```

---

### 五、完整调用链路汇总

```
[阶段一] 创意生成页面 → 生成全局大纲
  OutlineGenerator.generate_global_outline()
    → LLM生成全局大纲文本（包含人物、世界观、分章规划）
    → build_global_outline_graph() 构建知识图谱

[阶段一] 创意生成页面 → 生成单元概述
  OutlineGenerator.generate_all_chapters_atomic()
    ├── ChapterBoundaryMixin.extract_chapter_boundaries()
    │     → 定位"单元内容规划"段落
    │     → 解析范围行/单章行
    │     → 返回 {章节号: 边界描述}
    │
    └── 逐章循环:
          ├── _build_atomic_chapter_prompt()
          │     ├── _extract_chapter_outline_segment()  ← 正则隔离本章大纲
          │     ├── build_boundary_context_for_chapter() ← 边界上下文
          │     ├── _build_locked_summary()  ← 前5章摘要
          │     └── 传入完整 global_outline  ← v5.0: 不再用GraphRAG
          │
          ├── _call_llm_with_retry()  ← LLM生成（含429/网络断联重试）
          │
          ├── parse_unit_summaries()  ← 解析LLM输出
          │
          ├── validate_boundary_semantic()  ← 两级边界验证
          │     ├── _keyword_prefilter()  ← 关键词预筛
          │     └── _llm_semantic_check()  ← LLM语义验证（按需触发）
          │
          └── locked_chapters[ch_key] = chapter_data  ← 锁定

[阶段二] 写作工作台 → 逐章生成正文
  ContextBuilderMixin._build_context()
    ├── 从知识图谱加载人物设定: knowledge_graph.get_character_profiles()
    ├── 从知识图谱加载世界观: knowledge_graph.get_world_settings()
    ├── 回退到 outline 结构化字段: _extract_characters_from_outline()
    └── 组装 AgentContext（outline + character_profiles + unit_summaries + ...）

  UnitDirectMixin._process_unit_direct(unit_index=N)
    ├── CharacterStateTracker.get_state_for_prompt(chapter_num=N)
    ├── CharacterStateTracker.get_knowledge_graph_context_for_writing(N)
    ├── NovelKnowledgeGraph.format_consistency_report_for_prompt(N)
    ├── 合并 → full_kg_context
    │
    └── WriterAgent.execute(writer_context)
          └── _build_direct_writer_user_prompt()
                ├── [全局故事背景]     ← global_outline 完整文本
                ├── [本章创作指南]     ← unit_title + unit_summary  
                ├── [前文知识图谱参考]  ← full_kg_context
                ├── [人物状态追踪]     ← character_state_snapshot
                ├── [人物关系链]       ← relationship_summary
                ├── [人物当前位置]     ← character_location_map
                ├── [人物身份/官职]    ← character_identity_map
                └── [前文+衔接要求]    ← previous_content (3000+500窗口)
```

---

### 六、关键设计原则总结

1. **正向约束而非负向禁止**：告诉 LLM "本章应该写什么"（通过边界描述 + 章节大纲段落），而非"不要写什么"
2. **完整大纲传递（v5.0）**：不再通过 GraphRAG 检索片段，而是直接传入完整 `global_outline`，避免细粒度信息丢失
3. **章节内容隔离**：通过正则 `_extract_chapter_outline_segment()` 精准提取本章大纲，禁止字符串粗放切片
4. **两级边界验证**：关键词预筛（快速） + LLM 语义验证（精确），只有预筛发现疑点才触发昂贵的 LLM 调用
5. **滚动锁定上下文**：前 5 章摘要 + 上一章结尾 + 下一章边界 → 确保叙事连贯
6. **28 个 Mixin 的状态追踪器**：按功能域拆分为独立 Mixin，每个单一职责，组合成完整的 CharacterStateTracker