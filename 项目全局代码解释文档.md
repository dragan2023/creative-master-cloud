# 全能创意大师 项目全局代码解释文档

> **版本**: v4.2  
> **生成日期**: 2026-05-10  
> **说明**: 本文档仅对项目代码进行解释说明，**绝不修改任何代码**。

---

## 目录

- [一、项目架构说明](#一项目架构说明)
- [二、核心文件功能解释](#二核心文件功能解释)
- [三、关键代码逻辑解释](#三关键代码逻辑解释)
- [四、工作机制与组件解释](#四工作机制与组件解释)

---

## 一、项目架构说明

### 1.1 整体架构概览

本项目是一个**基于 AI 多 Agent 协作的创意内容生成平台**，支持短篇创意生成（短视频脚本、广告文案、电影大纲等）与**长篇文学作品创作**（小说、剧本、分集大纲等）。系统采用前后端分离架构，后端基于 Python FastAPI，前端基于 Vue 3 + Vite。

```
┌──────────────────────────────────────────────────────────┐
│                      Nginx 反向代理                        │
│              (生产环境: 路由转发 / Gzip / SSL)              │
└──────────────┬───────────────────────────┬───────────────┘
               │                           │
┌──────────────▼───────────┐ ┌─────────────▼──────────────┐
│    前端 (Vue 3 + Vite)    │ │    后端 (FastAPI + Uvicorn) │
│   ├── views/  页面层      │ │   ├── api/   路由层         │
│   ├── stores/ 状态管理    │ │   ├── services/  业务层    │
│   ├── api/    API调用层   │ │   ├── agents/    智能体层  │
│   ├── components/ 组件层  │ │   ├── models/   数据模型层 │
│   └── config/  配置层     │ │   ├── schemas/  校验层     │
│                           │ │   ├── core/     基础设施层 │
│                           │ │   └── tools/    工具层     │
└───────────────────────────┘ └─────────────┬──────────────┘
                                            │
                      ┌─────────────────────┼─────────────────────┐
                      │                     │                     │
              ┌───────▼──────┐    ┌────────▼────────┐   ┌───────▼──────┐
              │  PostgreSQL  │    │     Redis        │   │   ChromaDB   │
              │  (主数据库)   │    │  (缓存/会话)      │   │  (向量存储)   │
              └──────────────┘    └─────────────────┘   └──────────────┘
```

### 1.2 分层架构（后端）

| 层级 | 目录 | 职责 | 依赖方向 |
|------|------|------|----------|
| **路由层** | `app/api/` | REST API 端点定义，请求参数校验，响应格式化 | → 服务层 + Schema层 |
| **Schema层** | `app/schemas/` | Pydantic 数据模型，请求/响应验证 | 独立 |
| **服务层** | `app/services/` | 核心业务逻辑，编排调用 Agent 和工具 | → Agent层 + 工具层 + 模型层 |
| **Agent层** | `app/agents/` | AI 智能体（LLM调用、多Agent协作） | → 核心层 + 工具层 |
| **模型层** | `app/models/` | SQLAlchemy ORM 模型，数据库表映射 | → 核心层 |
| **仓库层** | `app/repositories/` | 数据访问抽象，封装数据库操作 | → 模型层 |
| **工具层** | `app/tools/` | 通用工具（搜索、文件解析、GraphRAG等） | → 核心层 |
| **核心层** | `app/core/` | 基础设施（配置、数据库、安全、日志） | 独立 |
| **中间件层** | `app/middleware/` | HTTP 请求拦截（安全消毒、大小限制） | → 核心层 |

### 1.3 前端分层架构

| 层级 | 目录 | 职责 |
|------|------|------|
| **视图层** | `src/views/` | 页面级组件，按功能模块组织 |
| **布局层** | `src/layouts/` | 全局布局（侧边栏 + 顶栏 + 内容区） |
| **路由层** | `src/router/` | 路由定义 + 导航守卫（权限控制） |
| **状态层** | `src/stores/` | Pinia 状态管理（用户、任务、大纲等） |
| **API层** | `src/api/` | Axios 封装，后端 API 调用 |
| **配置层** | `src/config/` | 前端配置（模块注册、LLM提供商列表） |
| **组合式函数** | `src/composables/` | 可复用逻辑（新手引导、生成恢复） |

### 1.4 模块依赖关系

后端的核心模块依赖关系如下图所示：

```
api/ ──依赖──▶ services/ ──依赖──▶ agents/ ──依赖──▶ core/
  │               │                    │
  └──依赖──▶ schemas/          └──依赖──▶ tools/
                  │
                  └──依赖──▶ models/ ──依赖──▶ core/
```

所有层级最终都依赖 `app/core/` 提供的基础设施（配置、数据库连接、安全模块、日志系统）。

---

## 二、核心文件功能解释

### 2.1 后端核心文件

#### 2.1.1 应用入口

| 文件 | 路径 | 职责说明 |
|------|------|----------|
| `main.py` | `backend/app/main.py` | **FastAPI 应用入口**。负责应用初始化、生命周期管理（启动时校验密钥、初始化数据库、创建超级管理员、清理僵尸任务、ChromaDB健康检查；关闭时清理运行中任务）、中间件注册（请求大小限制、输入消毒、CORS、请求计时）、路由挂载（`/api/v1` 前缀）、WebSocket端点注册、生产环境前端SPA托管 |

#### 2.1.2 核心基础设施 (`app/core/`)

| 文件 | 职责说明 |
|------|----------|
| `config.py` | **全局配置管理**。使用 `pydantic-settings` 基于环境变量/`.env` 文件加载所有配置项，包括：JWT密钥、数据库连接URL、Redis配置、LLM API密钥（DeepSeek/OpenAI/Google/千问/豆包/硅基流动等8个提供商）、ChromaDB向量库路径、CORS策略、上传限制、GPU加速开关、MCP配置等。提供路径规范化方法 |
| `config_presets.py` | **预设常量化**。定义预置 LLM 模型列表（`PRESET_MODELS`）和搜索服务商配置（`SEARCH_PROVIDERS`），供前端配置页面下拉选择 |
| `database.py` | **数据库连接管理**。使用 SQLAlchemy 2.0 异步引擎，支持 PostgreSQL (asyncpg) 和 SQLite (aiosqlite) 双模式。提供 `init_db()` 初始化表结构、`run_migrations()` 执行 Alembic 迁移、`get_db()` 异步会话工厂 |
| `security.py` | **安全模块**。提供 bcrypt 密码哈希/校验、JWT Token 创建(`create_access_token`)与解码验证、Fernet 对称加密（用于API Key加密存储） |
| `redis_client.py` | **Redis 客户端**。支持标准 Redis 连接和 `memory://` 内存降级模式（Redis不可用时自动切换 `MemoryStorage`），提供统一的缓存读写接口 |
| `exceptions.py` | **统一异常体系**。定义 `AppException` 基类及6个子异常：`AuthenticationException`、`ValidationException`、`GenerationException`、`LLMServiceException`、`TaskCancelledException` 等，附带 `ErrorCode` 枚举（14种错误码） |
| `logger.py` | **日志系统**。基于 Loguru，提供控制台彩色输出 + 按日轮转文件日志（30天保留），支持 Agent 调试日志独立配置 |
| `sse_ticket.py` | **SSE 票据管理**。为 Server-Sent Events 流式推送提供一次性票据验证，确保 SSE 连接安全 |
| `tenant_context.py` | **多租户上下文**。管理 SaaS 多租户隔离的当前租户ID上下文变量，通过 `ContextVar` 实现请求级隔离 |
| `module_registry.py` | **模块注册表**。管理创意生成模块的注册与发现 |
| `vector_store/` | **ChromaDB 向量存储封装**。基于 ChromaDB 持久化向量存储，使用 `BAAI/bge-small-zh-v1.5` 中文嵌入模型，提供文档向量化与语义检索能力 |

#### 2.1.3 API 路由层 (`app/api/`)

| 路由前缀 | 文件/目录 | 职责说明 |
|----------|-----------|----------|
| `/api/v1/auth` | `endpoints/auth/` | 用户认证（注册/登录/登出）、API Key CRUD、代理配置、知识库预处理配置 |
| `/api/v1/tenant-auth` | `endpoints/tenant_auth.py` | 租户认证（租户注册、登录、配额查询、套餐管理） |
| `/api/v1/admin` | `endpoints/admin.py` | 后台管理（用户管理、租户管理、系统日志查看、统计面板） |
| `/api/v1/generate` | `endpoints/generate/` | **创意生成核心API**。短视频脚本、电影大纲、剧集大纲、小说大纲、广告脚本等模块的生成和流式SSE输出。含多模态文件上传、大纲两阶段生成、原创IP修订 |
| `/api/v1/knowledge` | `endpoints/knowledge/` | 知识库 CRUD、搜索、知识图谱、文件上传处理、状态管理 |
| `/api/v1/novel-writer` | `endpoints/novel_writer/` | **小说/剧本写作核心API**。项目管理、大纲管理、章节管理、内容生成触发、知识库关联、风格文档管理、质量管控 |
| `/api/v1/writing-tasks` | `endpoints/writing_tasks/` | 写作任务管理（CRUD、中断、续传、状态查询、统计、导出） |
| `/api/v1/agent-config` | `endpoints/agent_config/` | Agent 模型配置管理（各Agent的LLM模型选择） |
| `/api/v1/writing-model-configs` | `endpoints/writing_model_config/` | 写作模型预配置方案管理（保存/加载用户的模型组合配置） |
| `/api/v1/update` | `endpoints/update.py` | 系统版本更新检查与备份 |
| `/api/v1/mcp` | `endpoints/mcp.py` | MCP 多内容提供商（热点新闻、趋势数据检索） |
| `api/deps.py` | `api/deps.py` | **依赖注入模块**。提供 JWT Bearer Token 认证（支持 Header/Cookie/Query参数三种方式）、多租户上下文自动设置、权限分层注入函数（`get_current_user` → `get_current_active_user` → `get_current_superuser`） |

#### 2.1.4 数据模型层 (`app/models/`)

| 模型文件 | 对应数据库表 | 职责说明 |
|----------|-------------|----------|
| `base.py` | (混入基类) | `BaseModel`（自增ID主键）+ `TimestampMixin`（自动创建/更新时间），所有模型继承此基类 |
| `user.py` | `users` | 用户表：用户名、邮箱、密码哈希、角色（USER/TENANT_ADMIN/SUPER_ADMIN）、租户关联、激活状态 |
| `tenant.py` | `tenants` | 租户表：租户名称、状态（ACTIVE/SUSPENDED/DELETED）、套餐（FREE/BASIC/PRO/ENTERPRISE）、配额限制 |
| `api_key.py` | `user_api_keys` | 用户API密钥：加密存储LLM提供商密钥，支持自定义Provider |
| `generation.py` | `generations` + `generation_revision_history` | 创意生成记录：模块类型、输入参数、输出内容、状态追踪、修订历史 |
| `knowledge_base.py` | `knowledge_bases` | 知识库：名称、类型（TEMP/STATIC/API）、分类、文件路径、向量存储ID、处理状态 |
| `prompt_template.py` | `prompt_templates` | 提示词模板：模块关联、系统提示词内容、激活状态 |
| `novel_project.py` | `novel_projects` | **小说/剧本项目**（核心模型）：项目名称、内容类型（novel/series/movie）、全局大纲、单元概述、风格配置、合规配置、章节总数 |
| `novel_chapter.py` | `novel_chapters` | 小说章节：项目关联、序号、标题、草稿内容、最终内容、编辑历史、字数统计 |
| `writing_task.py` | `writing_tasks` | **写作任务**：UUID标识、项目关联、任务类型、状态（PENDING/RUNNING/COMPLETED/FAILED）、进度追踪、配置JSON |
| `writing_unit.py` | `writing_units` | 写作单元：单元级内容、状态、质量管控字段（逻辑评分/风格评分/合规评分） |
| `writing_scene.py` | `writing_scenes` | 写作场景：多Agent处理结果（Writer原始输出、Editor修改后、Stylist润色后、Compliance审查结果） |
| `writing_checkpoint.py` | `writing_checkpoints` | 检查点：任务中断时的状态快照，支持断点续传 |
| `writing_stat.py` | `writing_stats` | 写作统计：各Agent的Token消耗、调用次数、耗时统计 |
| `writing_model_config.py` | `writing_model_configs` | 写作模型预配置：用户保存的模型组合方案（7个Agent角色各自的模型选择） |
| `quality_report.py` | `quality_reports` | **质量分析报告**：六维度质量评分（结构/人物/场景/文笔/体验/技术）、问题列表、改进建议 |
| `operation_log.py` | `operation_logs` | 操作日志：审计追踪（用户操作、IP、时间） |
| `system_config.py` | `system_configs` | 系统配置：键值对存储 |
| `system_version.py` | `system_versions` | 系统版本管理 |

#### 2.1.5 Agent 智能体层 (`app/agents/`)

##### LLM 提供商模块

| 文件 | 职责说明 |
|------|----------|
| `base_provider.py` | **LLM 提供商抽象基类**。定义 `BaseLLMProvider` 抽象接口（`generate()`、`generate_stream()` 方法），以及通用数据结构 `LLMResponse`（content/token_usage/finish_reason）、`MultimodalMessage` |
| `deepseek_provider.py` | DeepSeek 官方 API 实现，支持思考模式（reasoning_content） |
| `openai_provider.py` | OpenAI 兼容接口实现，同时用于 SiliconFlow、OpenRouter、T8Star 等兼容提供商 |
| `qianwen_provider.py` | 通义千问（阿里云百炼）API 实现 |
| `google_provider.py` | Google Gemini API 实现 |
| `doubao_provider.py` | 豆包（火山引擎）API 实现 |
| `llm_manager.py` | **LLM 管理器单例**。统一创建和管理各Provider实例，支持从数据库读取用户API密钥、解密失败自动降级到系统预置密钥、全局回退密钥链机制 |
| `memory_manager.py` | **记忆管理器**。短期记忆（Redis/内存）+ 长期记忆（ChromaDB向量库），为Agent提供上下文记忆能力 |
| `prompt_manager/` | **提示词管理器**。从数据库加载激活模板，支持智能变量填充（用户值 → 前端字段映射 → 默认值），管理 `MODULE_VARIABLES_CONFIG` 变量配置 |
| `prompt_optimizer.py` | **提示词优化器**。将用户笼统的创意描述转化为结构化的详细提示词，提升生成质量 |

##### 创意生成编排器 (`agents/orchestrator/`)

| 文件/目录 | 职责说明 |
|-----------|----------|
| `api/__init__.py` | 公共接口定义（转换工具函数、流式上下文数据结构） |
| `impl/generator.py` | **`AgentOrchestrator` 工厂函数**。最顶层的生成编排器，负责协调 LLM Provider、工具链和记忆系统完成各种创意生成任务 |
| `impl/mixins/generation_core.py` | 核心生成逻辑 |
| `impl/mixins/generate_stream_mixin.py` | 流式生成 SSE 推送 |
| `impl/mixins/generate_sync.py` | 同步生成模式 |
| `impl/mixins/knowledge_retrieval.py` | 知识检索集成 |
| `impl/mixins/context_utils.py` | 上下文构建工具 |
| `impl/mixins/evaluation.py` | 内容评估 |
| `impl/mixins/reflection.py` | 反思/迭代优化 |
| `impl/mixins/revision.py` | 修订功能 |
| `impl/mixins/session.py` | 会话管理 |

##### 多Agent写作系统 (`agents/writing/`)

这是整个项目最核心的子系统，实现多 Agent 协作的长篇文学作品创作。

| Agent | 文件/目录 | 角色定位 | LLM温度 | 职责说明 |
|-------|-----------|----------|---------|----------|
| **OrchestratorAgent** | `orchestrator_agent/` | 总调度 | 默认 | 任务调度、内容流水线编排、Agent间通信协调、单元/场景级别的生成控制 |
| **WriterAgent** | `writer_agent/` | 写手 | 0.8（高创意） | 根据大纲创作文学内容，支持整章直写模式。构建层级化提示词（全局故事背景→风格文档→单元概述→知识图谱上下文→人物状态→前文内容→字数要求→输出格式） |
| **KnowledgeAgent** | `knowledge_agent/` | 知识顾问 | 0.3（高准确性） | 提供上下文知识检索。四路并行检索：向量存储检索 + 知识图谱检索 + 角色关系获取 + 剧情线索获取，输出格式化的上下文摘要 |
| **LogicEditorAgent** | `logic_editor/` | 逻辑编辑 | 0.2（低随机） | 两阶段逻辑审查：检测阶段（情节连贯性、角色行为一致性、时间线、场景矛盾、对话匹配）→ 修正阶段（自动修正medium及以上严重度问题）。集成知识图谱获取扩展实体一致性上下文 |
| **StyleEditorAgent** | `style_editor/` | 风格润色 | 0.6 | 三阶段处理：AI文风检测 → 人性化改写（消除AI痕迹）→ 风格润色（精准风格指南或通用优化）。提供文风文档分析功能 |
| **ComplianceAgent** | `compliance_agent.py` | 合规审查 | - | **双层审查**：第一层Trie树本地敏感词检测（快速无网络开销）→ 第二层LLM辅助判断（上下文敏感内容）。评分机制：high扣30分/medium扣10分/low扣5分，≥80分且无high违规即通过 |
| **AssemblerAgent** | `assembler_agent.py` | 合成器 | - | 将一个Unit下所有Scene合并为完整章节。默认不需要LLM（纯文本拼接），可选启用LLM智能衔接优化 |
| **CharacterStateTracker** | `character_state_tracker/` | 角色状态跟踪 | - | 管理角色档案、检测新角色出现、追踪关系变化、记录章节快照、生成一致性报告。支持知识图谱导出 |

##### Agent 基础设施

| 文件/目录 | 职责说明 |
|-----------|----------|
| `base_agent/_types.py` | 定义核心数据类型：`AgentRole`（8种Agent角色枚举）、`AgentContext`（44个字段的标准Agent间通信数据结构）、`AgentResult`（标准返回结构：content/data/errors/warnings/token_usage） |
| `base_agent/_llm.py` | `AgentLLMMixin`：封装LLM调用，支持自动重试（最多3次、指数退避）、Provider自动推断、流式调用、Token统计记录 |
| `base_agent/_execution.py` | `AgentExecutionMixin`：系统提示词构建、成功/错误结果构建、`requires_llm`标志控制 |
| `agent_config.py` | Agent模型配置管理（各Agent使用的LLM模型和密钥配置） |
| `stats_interceptor.py` | Token统计拦截器（记录各Agent的LLM消耗） |
| `prompts/` | 所有Agent的提示词模板（writer/editor/structural/style/compliance/knowledge/assembler/virtual_mode等10+文件） |

#### 2.1.6 业务服务层 (`app/services/`)

| 文件/目录 | 职责说明 |
|-----------|----------|
| `writing_engine/` | **写作引擎核心**。`TaskManager`（写作任务CRUD服务）、`WritingPipeline`（多Agent流水线编排）、`WebSocketManager`（实时状态推送）。详见[工作机制章节](#四工作机制与组件解释) |
| `novel_writer/` | **小说/剧本生成服务包**。上下文窗口管理（`ContextWindowManager`）、语义压缩（`SemanticCompressor`分级压缩）、一致性管理（`ConsistencyManager`）、项目向量存储（`ProjectVectorStore`）、项目知识库（`ProjectKnowledgeBase`）、GraphRAG增强上下文、策略模式（Novel/SeriesScript/MovieScript三种策略）、项目健康检查器、章节同步服务。详见[工作机制章节](#四工作机制与组件解释) |
| `quality_control/` | **六维度质量管控**。宏观结构/人物塑造/场景感官/文笔修辞/阅读体验/技术排雷。双引擎架构：`RuleBasedEngine`（零Token代价的规则分析）+ `LLMAnalysisEngine`（深度语义分析）。支持quick/deep两种分析深度 |
| `outline_generator/` | **两阶段大纲生成**。通过17个Mixin组合实现：全局大纲生成 → 单元概述生成 → QC修正。支持流式输出和断点续传 |
| `task_manager.py` | 旧版批量生成任务状态管理（进度追踪、取消、持久化、SSE推送），与writing_engine中的TaskManager为不同版本 |
| `generation_service.py` | 创意生成通用服务 |
| `knowledge_base_service.py` | 知识库综合服务（CRUD、搜索、知识图谱构建、文件处理） |
| `ip_plan_generator.py` | 原创IP计划生成器 |
| `admin_service.py` | 后台管理服务 |
| `compliance/auditor.py` | 合规审核服务（非阻塞标记） |
| `proofread/` | 校对工具集（敏感词检测Trie树、文档格式化、章节识别） |
| `alerting.py` | 告警服务 |
| `monitoring.py` | 监控服务 |
| `user_action_service.py` | 用户行为追踪服务 |

#### 2.1.7 工具层 (`app/tools/`)

| 文件/目录 | 职责说明 |
|-----------|----------|
| `web_search.py` | 网络搜索工具（关键词提取 → 搜索引擎调用 → 结果格式化） |
| `knowledge_retrieval.py` | 知识检索工具（从ChromaDB向量库检索相关知识） |
| `file_parser.py` | 文件解析器（支持 PDF/DOCX/TXT/Markdown 格式） |
| `webpage_reader.py` | 网页内容读取（URL抓取 → HTML解析 → 正文提取） |
| `doc_preprocessor.py` | 文档预处理流水线（语义分块、摘要生成、元数据提取） |
| `semantic_chunker.py` | 语义分块器（基于语义边界的智能文本分割） |
| `graph_rag/` | **GraphRAG**（知识图谱实体提取 + 双轨检索：向量检索 + 图谱检索） |
| `novel_graph_rag/` | **小说专用GraphRAG**（角色提取、关系提取、一致性报告、角色档案格式） |
| `mcp/` | **MCP 多内容提供商**（热点新闻、趋势数据，含客户端、缓存、配置管理） |
| `creative_search/` | 创意搜索（关键词提取 → 搜索引擎 → 质量评估 → 格式化输出） |
| `style_library/` | 风格库（叙事风格/个人风格/传统风格/网文风格的JSON数据定义） |
| `proxy_router.py` | 代理路由器 |

### 2.2 前端核心文件

#### 2.2.1 入口与配置

| 文件 | 路径 | 职责说明 |
|------|------|----------|
| `main.js` | `frontend/src/main.js` | **前端应用入口**。创建Vue应用实例，注册Element Plus（中文语言包）、Pinia状态管理、Vue Router、全局组件 |
| `App.vue` | `frontend/src/App.vue` | 根组件，仅包含 `<router-view />` 路由出口 |
| `index.html` | `frontend/index.html` | HTML模板（标题"全能创意大师"、加载 Google Fonts、入口脚本指向 `/src/main.js`） |
| `vite.config.js` | `frontend/vite.config.js` | **Vite构建配置**。定义别名`@`→`src/`、开发服务器端口代理（`/api`→后端）、生产构建输出到`../backend/app/static`、代码分包（element-plus和g6独立）、可选代码混淆 |
| `package.json` | `frontend/package.json` | 前端依赖管理。核心依赖：Vue 3.5+、Element Plus 2.13+、Pinia 2.1+、ECharts 6.0+、AntV G6 5.0+、marked（Markdown渲染）、diff（对比）、DOMPurify（XSS过滤）、mammoth（Word解析）、sse.js（SSE流式） |

#### 2.2.2 路由配置

| 文件 | 职责说明 |
|------|----------|
| `router/index.js` | **主路由定义**。定义全部路由表（登录/注册/首页/创意生成/API Key/知识库/小说工作台/管理后台/404），注册全局导航守卫 `beforeEach` 实现：页面标题设置、已登录用户访问公开页面的重定向、未登录拦截（保存redirect参数）、管理员权限验证（`requiresAdmin`→需超级管理员、`requiresSuperAdmin`→需超级管理员） |
| `router/admin.js` | 管理员路由空壳文件，预留给未来按需加载拆分（当前管理员路由已全部在`index.js`中注册） |

#### 2.2.3 布局

| 文件 | 职责说明 |
|------|----------|
| `layouts/MainLayout.vue` | **主布局组件**。三段式结构：可折叠侧边栏（64px/220px，深色科技渐变背景，动态菜单含权限控制）、顶部栏（面包屑导航+用户下拉菜单）、主内容区（`<router-view>`+`key`强制重建）。底部显示从后端动态获取的版本号，`onMounted`时自动恢复用户信息和版本数据 |

#### 2.2.4 状态管理 (Pinia Stores)

| Store | 文件 | 职责说明 |
|-------|------|----------|
| `useUserStore` | `stores/user.js` | **用户认证状态**。管理 token/userInfo 的 localStorage 持久化，提供 login/register/logout/fetchProfile 操作，计算属性 isLoggedIn/isAdmin/isSuperAdmin |
| `useAppStore` | `stores/app.js` | **应用全局设置**。侧边栏折叠状态、暗色模式切换 |
| `useApiKeyStore` | `stores/apiKey.js` | API Key 管理（CRUD操作的状态管理） |
| `useTaskStore` | `stores/task.js` | 旧版任务状态管理 |
| `useWritingTaskStore` | `stores/writingTask.js` | **新版写作任务状态**（多Agent写作系统核心前端状态）。管理任务列表、轮询状态更新、WebSocket连接 |
| `useOutlineStore` | `stores/outline.js` | 大纲管理（分集/章节/场景结构的增删改查） |

#### 2.2.5 API 请求层

| 文件 | 职责说明 |
|------|----------|
| `api/_axios.js` | **Axios 实例与拦截器**。创建基础 `axios` 实例（baseURL、超时配置），请求拦截器自动附加 JWT Token，响应拦截器处理401自动跳转登录、统一错误处理 |
| `api/auth.js` | 认证API（login/register/logout/me） |
| `api/generate.js` | **创意生成API**（核心，16KB）。涵盖所有创意模块的生成请求，含流式SSE生成的多模块支持 |
| `api/novel-writer.js` | **小说/剧本API**（13KB）。项目管理、大纲、章节、内容生成触发、风格文档等全套接口 |
| `api/writing-task.js` | 写作任务API（CRUD、控制、查询） |
| `api/knowledge.js` | 知识库API |
| `api/history.js` | 历史记录API |
| `api/admin.js` | 管理后台API |
| `api/system.js` | 系统API（版本、配置） |
| `api/api-key.js` | API Key管理API |
| `api/mcp.js` | MCP协议API |
| `api/quality-control.js` | 质量管控API |
| `api/revision.js` | 修订API |
| `api/update.js` | 版本更新API |
| `api/user-config.js` | 用户配置API |

#### 2.2.6 配置层

| 文件 | 职责说明 |
|------|----------|
| `config/index.js` | **前端主配置**（13KB）。定义 API 基础地址、LLM 提供商列表、搜索引擎提供商列表、各创意模块的参数配置模板、默认设置 |
| `config/modules.js` | **创意模块统一注册表**（8KB）。集中注册所有创意生成模块（短视频脚本/电影大纲/剧集大纲/小说大纲/广告脚本/原创IP等），定义每个模块的图标、描述、参数Schema、默认提示词 |
| `config/version.js` | 版本号配置（与 `version.json` 同步） |

---

## 三、关键代码逻辑解释

### 3.1 后端关键代码逻辑

#### 3.1.1 `Settings` 配置类（`app/core/config.py`）

**作用**：全局配置单例，通过 `pydantic-settings` 从环境变量和 `.env` 文件加载所有配置。

**关键参数逻辑**：
- **数据库配置**：`DATABASE_URL` 默认指向 PostgreSQL（`postgresql+asyncpg://`），可通过环境变量切换为 SQLite（`sqlite+aiosqlite://`），自动检测并适配
- **LLM 密钥管理**：定义了 8 个 LLM 提供商的 API Key 字段（`DEEPSEEK_API_KEY`、`OPENAI_API_KEY`、`GOOGLE_API_KEY`、`QIANWEN_API_KEY`、`DOUBAO_API_KEY` 等），每个字段带有环境变量别名映射（支持多种环境变量名）
- **路径规范化**：`get_chroma_path()`、`get_upload_path()` 等方法自动将相对路径转为绝对路径，确保在不同工作目录下一致
- **批量生成速率控制**：定义 `GENERATION_INTERVAL_SECONDS`（请求间隔）、`MAX_RETRIES`（重试次数）、`RETRY_BACKOFF_FACTOR`（指数退避因子）等参数

#### 3.1.2 `BaseLLMProvider` 抽象基类（`app/agents/base_provider.py`）

**作用**：定义所有 LLM 提供商的统一接口标准。

**关键方法签名与逻辑**：

- `generate(messages, model, temperature, max_tokens, **kwargs) -> LLMResponse`
  - 接收标准化的消息列表，返回统一的 `LLMResponse` 对象
  - `LLMResponse` 包含 `content`（生成文本）、`token_usage`（Token统计）、`finish_reason`（完成原因）
  
- `generate_stream(messages, model, temperature, max_tokens, **kwargs) -> AsyncGenerator[str, None]`
  - 异步生成器，逐步 yield 生成的文本片段（用于 SSE 流式输出）

- `MultimodalMessage` 数据类型
  - 支持文本+图片的多模态消息格式

#### 3.1.3 `LLMManager` 管理器（`app/agents/llm_manager.py`）

**作用**：LLM 提供商的统一创建和管理单例。

**关键逻辑**：

1. **Provider 注册映射**：内部维护一个 Provider 注册表，将字符串标识（如 `"qianwen"`）映射到具体的 Provider 类（如 `QianwenProvider`）
2. **密钥获取优先级**：用户数据库存储的加密密钥 → 系统环境变量密钥 → 全局回退密钥链
3. **密钥解密与降级**：从数据库读取的密钥使用 Fernet 解密，解密失败时自动降级到系统预置密钥，确保服务可用性
4. **思考模式支持**：DeepSeek Provider 额外支持 `reasoning_content` 字段，在流式输出中分离思考过程和最终回答

#### 3.1.4 `WritingPipeline` 流水线（`app/services/writing_engine/pipeline/`）

**设计模式**：Mixin 多重继承，`WritingPipeline` 本身为空类，所有行为通过 5 层 Mixin 继承链注入：

```
WritingPipeline → PipelineControlMixin → PipelineExecuteMixin → ContextBuilderMixin → PipelineConfigMixin → PipelineBase
```

**`_execute()` 核心执行方法**（`PipelineExecuteMixin`）：

1. 在独立数据库会话中创建异步上下文
2. **安全检查：校准 total_units**（防御性代码）
   - 检测 `total_units` 为 0/None → 从 `unit_summaries` / `NovelChapter` 表 / `project.total_chapters` 多级回退查询
   - 检测 `unit_summaries` 实际长度大于 `total_units` → 动态校准修正
3. 创建 `OrchestratorAgent`，注入 stats_interceptor（统计记录器）和 ws_manager（WebSocket管理器）
4. 更新任务状态 PENDING → RUNNING，通过 WebSocket 推送通知
5. 调用 `_build_context()` 构建完整的 `AgentContext`（大纲、人物设定、风格配置等）
6. 调用 `self._orchestrator.execute(context)` 触发多 Agent 写作流程
7. 处理结果（COMPLETED/FAILED），WebSocket 推送最终状态和统计信息

**关键设计点**：
- 使用 `asyncio.create_task()` 将 `_execute()` 作为后台任务，`start()` 方法非阻塞返回
- `finally` 块确保无论成功失败都从 `_active_pipelines` 类变量移除
- 三层异常处理：`asyncio.CancelledError`（中断）、`AppException`（业务异常）、`Exception`（未知异常）

#### 3.1.5 `OrchestratorAgent` 任务调度（`app/agents/writing/orchestrator_agent/`）

**调度机制**：
- 负责将写作任务拆分为 Unit（单元）→ Scene（场景）两级粒度
- 为每个 Scene 编排 WriterAgent → LogicEditorAgent → StyleEditorAgent → ComplianceAgent 的处理链
- 通过 `AgentCommunication` 模块在各 Agent 间传递 `AgentContext` 和收集 `AgentResult`
- 最终通过 `AssemblerAgent` 将多个 Scene 合并为完整章节

#### 3.1.6 `ContextWindowManager` 上下文构建（`app/services/novel_writer/context_manager/`）

**作用**：为每个章节的生成构建完整的上下文信息。

**`build_chapter_context()` 方法的关键优化**：
- 使用 `asyncio.gather` 将原本串行的 10 步 IO 操作并行化为 3 组执行
- **第一组（并行）**：前文摘要、角色状态、近章内容、向量检索、知识库查询、大纲元信息
- **第二组（依赖第一组）**：单元概述、当前大纲、剧集大纲
- **第三组**：上下文压缩

#### 3.1.7 `SemanticCompressor` 语义压缩（`app/services/novel_writer/semantic_compressor.py`）

**分级压缩策略**：

| 压缩级别 | 距离范围 | 目标字数 | 保留内容 |
|----------|----------|----------|----------|
| DETAILED | 相邻 1-2 单元 | ~800字 | 完整结尾 + 关键情节 |
| MODERATE | 相隔 3-5 单元 | ~400字 | 事件摘要 + 角色变化 |
| BRIEF | 相隔 5+ 单元 | ~150字 | 核心事件和伏笔 |

**核心设计原则**：
- **禁止截断**：对正文生成结果禁止截断；对 `global_outline` 和 `unit_summaries` 禁止字符串切片
- **选择性传入**：大纲/概述通过 `select_unit_summaries()` 方法选择性传入来控制规模
- **LLM 语义压缩优先**：对前文摘要/向量检索结果优先使用 LLM 语义压缩，失败时降级为规则压缩
- **MD5 缓存**：相同内容的压缩结果带缓存，避免重复调用 LLM

#### 3.1.8 六维度质量管控（`app/services/quality_control/`）

**双引擎架构**：

| 引擎 | 机制 | Token消耗 | 适用场景 |
|------|------|-----------|----------|
| `RuleBasedEngine` | 基于正则/统计的规则分析（词频、感官词汇、段落结构、被动语态等） | 0 | 所有场景的基础扫描 |
| `LLMAnalysisEngine` | 深度语义分析（情节节奏、角色一致性、章末悬念等） | 有消耗 | deep模式的核心分析 |

**六个质量维度**：

| 维度 | 分析器 | 分析内容 |
|------|--------|----------|
| 宏观结构层 | `StructureAnalyzer` | 章节结构、节奏控制、伏笔管理 |
| 人物塑造层 | `CharacterAnalyzer` | 角色弧光、性格一致性、出格(OOC)检测 |
| 场景与感官 | `SceneAnalyzer` | 场景描写、感官细节、时空逻辑 |
| 文笔与修辞 | `ProseAnalyzer` | 文笔流畅度、修辞手法、语言风格 |
| 阅读体验 | `ExperienceAnalyzer` | 可读性、节奏感、章节悬念 |
| 技术排雷 | `TechnicalAnalyzer` | 常识错误、世界观一致性、Bug检测 |

**执行流程**：

1. `RuleBasedEngine.analyze_all()` — 规则引擎全维度扫描（零Token）
2. 若 `depth == "quick"` → 直接从规则结果构建报告
3. 否则 → 对 `structure/character/experience` 维度并发调用 LLM 引擎
4. 合并规则结果和 LLM 结果，计算综合评分
5. 支持基于内容哈希的缓存去重

#### 3.1.9 `AgentContext` 数据结构（`app/agents/writing/base_agent/_types.py`）

**作用**：Agent 间通信的唯一标准数据结构，包含 44 个字段。

**核心字段分类**：

| 分类 | 字段示例 | 说明 |
|------|----------|------|
| 任务标识 | `task_id`, `project_id`, `unit_index` | 定位当前写作进度 |
| 大纲信息 | `global_outline`, `unit_summaries`, `current_outline` | 大纲层级数据 |
| 角色信息 | `character_profiles`, `character_states` | 角色设定与运行时状态 |
| 风格配置 | `style_document_features`, `style_guide`, `ai_elimination_enabled` | 风格控制参数 |
| 前文上下文 | `previous_summary`, `recent_content`, `retrieved_context` | 历史内容引用 |
| 知识增强 | `knowledge_graph_context`, `vector_search_results` | 知识库检索结果 |
| 合规配置 | `compliance_config`, `sensitive_words` | 合规审查参数 |
| 内容类型 | `content_type`, `strategy_type` | 小说/剧集/电影区分 |

### 3.2 前端关键代码逻辑

#### 3.2.1 Axios 拦截器链（`api/_axios.js`）

**请求拦截器逻辑**：
1. 从 `userStore` 获取 JWT Token
2. 若 Token 存在，自动附加 `Authorization: Bearer <token>` 请求头
3. 支持多租户：自动附加 `X-Tenant-Id` 请求头

**响应拦截器逻辑**：
1. 检测 HTTP 401 状态码 → 清除登录状态 + 跳转 `/login`（携带当前路径作为 redirect 参数）
2. 统一错误格式化：提取 `response.data.detail` 或默认错误消息

#### 3.2.2 路由守卫权限控制（`router/index.js`）

**`beforeEach` 守卫逻辑**：

1. **页面标题设置**：`document.title = "{meta.title} - 全能创意大师"`
2. **公开页面处理**：已登录用户访问 `/login` 或 `/register` → 重定向到首页 `/`
3. **认证检查**：非公开页面且未登录 → 跳转 `/login?redirect=<当前路径>`
4. **管理员权限**：`meta.requiresAdmin` → 检查 `userStore.isSuperAdmin`（注释说明已改为仅超级管理员可访问）
5. **超级管理员权限**：`meta.requiresSuperAdmin` → 检查 `userStore.isSuperAdmin`，不满足则重定向到 `/admin`

#### 3.2.3 SSE 流式生成接口（`api/generate.js`）

**流式生成逻辑**：
- 前端通过 `sse.js` 库建立 SSE 连接
- 后端通过 `text/event-stream` 逐步推送生成内容
- 前端监听 `message` 事件累积内容，`end` 事件标记完成，`error` 事件处理异常

#### 3.2.4 小说工作台状态管理（`stores/writingTask.js`）

**轮询与WebSocket双通道更新**：
- WebSocket 连接用于实时推送任务状态变化（开始/进度/完成/失败）
- 轮询作为降级方案，在 WebSocket 不可用时定期查询任务状态
- 任务列表支持按项目过滤、状态筛选

---

## 四、工作机制与组件解释

### 4.1 短篇创意生成流程

```
用户输入创意描述
      │
      ▼
┌─────────────────┐
│ PromptOptimizer │  将笼统描述转化为结构化提示词
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  PromptManager  │  加载模块对应的提示词模板，填充变量
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   LLMManager    │  选择合适的LLM Provider，调用生成
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│AgentOrchestrator│  协调工具调用（知识检索、网络搜索等）
└────────┬────────┘
         │
         ▼
   流式 SSE 输出 → 前端实时渲染
```

**涉及的关键组件交互**：
- `PromptOptimizer` → `PromptManager` → `LLMManager` → `AgentOrchestrator` → API 路由 → SSE 推送 → 前端

### 4.2 长篇写作多Agent协作流程

```
用户创建项目 → 配置风格/知识库/模型
         │
         ▼
┌──────────────────────┐
│   OutlineGenerator   │  两阶段大纲生成
│  ① 生成全局详细大纲   │
│  ② 生成单元概述      │
│  ③ 质量管控修正      │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│    WritingPipeline   │  写作流水线启动
│   ① 创建WritingTask  │
│   ② 构建AgentContext │
│   ③ 调度Orchestrator │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────────────────┐
│                  OrchestratorAgent                     │
│                                                       │
│   按 Unit → Scene 粒度拆分                             │
│                                                       │
│   for each Scene:                                     │
│     ┌──────────────┐                                  │
│     │KnowledgeAgent│  ① 检索上下文知识                  │
│     └──────┬───────┘     (向量库+知识图谱+角色关系)       │
│            │                                           │
│            ▼                                           │
│     ┌──────────────┐                                  │
│     │ WriterAgent  │  ② 根据大纲+知识创作内容             │
│     └──────┬───────┘                                  │
│            │                                           │
│            ▼                                           │
│     ┌─────────────────┐                               │
│     │LogicEditorAgent │  ③ 检测逻辑一致性 + 自动修正     │
│     └──────┬──────────┘                               │
│            │                                           │
│            ▼                                           │
│     ┌─────────────────┐                               │
│     │StyleEditorAgent │  ④ AI文风检测→人性化改写→风格润色 │
│     └──────┬──────────┘                               │
│            │                                           │
│            ▼                                           │
│     ┌─────────────────┐                               │
│     │ComplianceAgent  │  ⑤ Trie本地检测 + LLM合规判断    │
│     └──────┬──────────┘                               │
│            │                                           │
│            ▼                                           │
│     持久化 Scene 结果到数据库                            │
│                                                       │
│   ┌──────────────────┐                                │
│   │ AssemblerAgent   │  ⑥ 所有Scene合并为完整章节        │
│   └──────────────────┘                                │
└──────────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────┐
│ QualityControlService│  六维度质量分析 + 反馈修正
└──────────┬───────────┘
           │
           ▼
    WebSocket → 前端实时展示进度和结果
```

### 4.3 Agent 间通信机制

**标准通信协议**：所有 Agent 通过两个核心数据结构进行通信：

1. **`AgentContext`**（输入）：包含 44 个字段的完整上下文信息，由 `ContextBuilderMixin._build_context()` 方法构建，从多个数据源（数据库、知识图谱、向量库、文件系统）聚合而成
2. **`AgentResult`**（输出）：包含 `content`（生成内容）、`data`（结构化数据）、`errors`（错误列表）、`warnings`（警告列表）、`token_usage`（Token统计）五个字段

**通信流程**：

```
ContextBuilderMixin._build_context()
    │
    ├── 从 NovelProject 加载全局大纲、单元概述
    ├── 从知识图谱文件加载角色档案、世界观设定
    ├── 从 ProjectVectorStore 检索前文相关内容
    ├── 从 ProjectKnowledgeBase 检索知识图谱上下文
    ├── 从 style_config 解析风格文档特征
    └── 构建 → AgentContext
                    │
                    ▼
            OrchestratorAgent
                    │
          ┌─────────┼─────────┐
          │         │         │
          ▼         ▼         ▼
    WriterAgent  EditorAgent  ...
          │         │         │
    ┌─────┴────┐    │         │
    │ AgentContext  │         │
    │  (传入)       │         │
    └─────┬────┘    │         │
          │         │         │
          ▼         ▼         ▼
    AgentResult  AgentResult  AgentResult
          │         │         │
          └─────────┼─────────┘
                    │
                    ▼
            OrchestratorAgent 收集汇总
```

### 4.4 实时通信机制

**双通道设计**：

| 通道 | 技术 | 用途 |
|------|------|------|
| SSE (Server-Sent Events) | `text/event-stream` | 短篇创意生成的流式内容推送 |
| WebSocket | `ws://` 协议 | 长篇写作任务的实时状态推送（进度、Agent执行状态、统计信息） |

**WebSocket 消息类型**：
- `status_change`：任务状态变更（PENDING → RUNNING → COMPLETED/FAILED）
- `progress_update`：进度更新（当前处理的 Unit/Scene 序号）
- `agent_start`：某个 Agent 开始执行
- `agent_complete`：某个 Agent 执行完成（附带 AgentResult 摘要）
- `stat_update`：Token 消耗统计更新

### 4.5 断点续传机制

**`WritingCheckpoint` 模型**：
- 在任务中断时保存完整的 `AgentContext` 快照和当前处理进度
- 恢复时从最近的 Checkpoint 加载状态，避免从头开始
- 支持多级回退（保留最近 N 个检查点）

**`PipelineControlMixin` 控制功能**：
- `interrupt()`：发送 `asyncio.CancelledError` 取消后台任务
- `resume()`：从检查点恢复执行
- 中断时自动清理临时文件和数据库状态

### 4.6 知识图谱集成机制

**双轨检索架构**：

```
用户查询/写作上下文
        │
        ├──────────────────┬──────────────────┐
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ 向量存储检索   │  │ 知识图谱检索   │  │ 角色关系检索   │
│(ChromaDB)     │  │(GraphRAG)     │  │(关系图谱)      │
│               │  │               │  │               │
│• 语义相似段落  │  │• 实体节点查询  │  │• 角色关联关系  │
│• 前文相关内容  │  │• 关系边遍历    │  │• 关系变化历史  │
│• 设定信息检索  │  │• 图谱路径推理  │  │• 一致性约束    │
└───────┬───────┘  └───────┬───────┘  └───────┬───────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                           ▼
                  ┌────────────────┐
                  │  结果融合格式化  │
                  │  去重+排序+摘要  │
                  └────────┬───────┘
                           │
                           ▼
                    Agent 上下文注入
```

**GraphRAG 组件**（`app/tools/graph_rag/` 和 `app/tools/novel_graph_rag/`）：
- **实体提取**：从文本中自动提取角色、地点、道具、事件等实体
- **关系提取**：识别实体间的交互关系（角色关系、从属关系、事件因果等）
- **图谱构建**：构建持久化的知识图谱（节点 + 边），支持增量更新
- **一致性报告**：检测角色状态变化、关系变更、设定冲突

### 4.7 策略模式消除内容类型差异

**三种内容策略**（`app/services/novel_writer/strategies/`）：

| 策略 | 适用类型 | 单元标签 | 编号字段 | 大纲数据源 |
|------|----------|----------|----------|-----------|
| `NovelStrategy` | 小说 | "章" | `chapter_number` | 章节大纲 |
| `SeriesScriptStrategy` | 剧集剧本 | "集" | `episode_number` | 分集大纲 |
| `MovieScriptStrategy` | 电影剧本 | "场" | `scene_number` | 场景大纲 |

**设计意图**：通过策略模式消除三种文学形式在代码中的 `if-else` 分支冗余。每个策略封装了特定内容类型的差异（标签命名、编号方式、数据源、正则提取规则等），上下文构建和Agent执行逻辑统一通过策略接口调用。

### 4.8 应用启动与生命周期

**`main.py` 的完整启动流程**：

```
1. 初始化日志系统 (Loguru)
2. SECRET_KEY 安全校验
   ├── 为空 → 拒绝启动 (Exit)
   └── 长度 < 32 → 警告但继续
3. 数据库初始化
   ├── 创建所有 SQLAlchemy 表
   └── 执行 Alembic 迁移 (alembic upgrade head)
4. 自动创建超级管理员 (从环境变量 ADMIN_USERNAME/ADMIN_PASSWORD/ADMIN_EMAIL)
5. 清理遗留的 RUNNING 状态任务 (僵尸进程清理)
6. ChromaDB 健康检查
   ├── 检测向量库数据丢失
   └── 自动修复
7. 注册中间件栈
   ├── RequestSizeLimitMiddleware (请求体大小限制)
   ├── InputSanitizationMiddleware (XSS/SQL注入防护)
   ├── CORSMiddleware (跨域支持)
   └── 请求计时中间件 (X-Process-Time header)
8. 注册全局异常处理器
   ├── AppException → 统一业务异常格式
   └── Exception → 通用异常兜底
9. 注册路由
   ├── /api/v1 → 所有业务API
   └── /api/v1/writing-tasks/{task_id}/ws → WebSocket
10. 前端 SPA 托管 (生产环境)
    └── /app/static/ → 前端构建产物
```

### 4.9 项目健康检查机制

**`ProjectHealthChecker`**（`app/services/novel_writer/health_checker.py`）：

生成前自动检查 7 个关键组件，按严重度分级：

| 严重度 | 组件 | 不可用时的行为 |
|--------|------|---------------|
| **critical** | 大纲 | 阻止生成，强制用户补充 |
| **warning** | 单元概述 | 提示但允许继续 |
| **info** | 详细大纲 | 降级跳过 |
| **info** | 知识库 | 无知识增强生成 |
| **info** | 向量库 | 无语义检索 |
| **info** | 摘要文件 | 重新生成 |
| **info** | 角色状态 | 从大纲重新提取 |

**设计意图**：分级降级策略确保系统鲁棒性——关键数据缺失时阻止执行防止质量问题，非关键数据缺失时降级处理保证可用性。

---

## 附录：项目技术栈总览

### 后端技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| Web框架 | FastAPI + Uvicorn | 异步 HTTP 服务 |
| ORM | SQLAlchemy 2.0 (async) | 数据库 ORM |
| 数据库 | PostgreSQL (asyncpg) / SQLite | 主数据存储 |
| 缓存 | Redis (可降级至内存) | 缓存与会话管理 |
| 向量数据库 | ChromaDB | 语义检索 |
| 嵌入模型 | BAAI/bge-small-zh-v1.5 | 中文文本向量化 |
| 认证 | python-jose (JWT) + bcrypt + Fernet | 用户认证与密钥加密 |
| LLM集成 | 多Provider统一接口 | AI 文本生成 |
| 实时通信 | SSE + WebSocket | 流式推送与状态同步 |
| 知识图谱 | 自研 GraphRAG | 实体提取与关系推理 |
| 配置管理 | pydantic-settings | 环境变量与配置 |
| 日志 | Loguru | 结构化日志 |
| 数据库迁移 | Alembic | Schema 版本管理 |

### 前端技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| 框架 | Vue 3 (Composition API) | UI 框架 |
| 构建 | Vite 6 | 开发与构建 |
| UI库 | Element Plus 2.13 | 组件库 |
| 状态管理 | Pinia 2.1 | 全局状态 |
| 路由 | Vue Router 4.4 | 页面路由 |
| HTTP | Axios | API 请求 |
| 图表 | ECharts 6.0 + AntV G6 5.0 | 数据可视化与知识图谱可视化 |
| Markdown | marked 17.0 | 内容渲染 |
| 文本对比 | diff 9.0 | 修订对比 |
| 安全 | DOMPurify 3.0 | XSS 过滤 |
| 文档解析 | mammoth 1.12 | Word 文档导入 |
| 实时通信 | sse.js 2.8 | SSE 流式接收 |
| CSS预处理 | Sass | 样式编写 |
| 代码保护 | rollup-plugin-obfuscator | 生产构建混淆 |

---

> **文档声明**：本文档为项目代码的全面解释说明，严格遵循"仅解释、不修改"的原则。所有代码路径、类名、方法名均来自实际项目结构，文档内容不包含任何对代码的修改建议。
