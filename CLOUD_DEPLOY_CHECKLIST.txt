# ========================================
# 全能创意大师 - 云端部署文件清单
# ========================================

## 一、环境架构说明

```
开发环境: run-local.bat（非容器化，SQLite，热更新）
    ↓ 代码推送
生产环境: docker-compose.prod.yml / docker-compose.cloud.yml（容器化，PostgreSQL）
```

## 二、根目录配置文件

| 文件 | 说明 | 必须 |
|------|------|------|
| docker-compose.prod.yml | 生产环境Docker编排 | ✅ |
| docker-compose.cloud.yml | 云端部署Docker编排 | ✅ |
| .env.cloud | 云端环境变量模板 | ✅ |
| .gitignore | Git忽略配置 | ✅ |

## 三、后端文件 (backend/)

### 3.1 核心配置
| 文件 | 说明 | 必须 |
|------|------|------|
| requirements.txt | Python依赖 | ✅ |
| Dockerfile | 开发环境Dockerfile | ✅ |
| Dockerfile.prod | 生产环境Dockerfile | ✅ |
| alembic.ini | 数据库迁移配置 | ✅ |
| .env | 本地开发环境变量 | 本地开发 |

### 3.2 应用代码 (backend/app/)
| 目录/文件 | 说明 | 必须 |
|-----------|------|------|
| main.py | FastAPI入口 | ✅ |
| __init__.py | 包初始化 | ✅ |
| agents/ | LLM提供者 | ✅ |
| api/ | API路由 | ✅ |
| core/ | 核心配置 | ✅ |
| models/ | 数据模型 | ✅ |
| schemas/ | 数据模式 | ✅ |
| services/ | 业务服务 | ✅ |
| tools/ | 工具模块 | ✅ |
| static/ | 前端静态文件 | ✅ |

### 3.3 数据库迁移 (backend/alembic/)
| 目录/文件 | 说明 | 必须 |
|-----------|------|------|
| env.py | 迁移环境配置 | ✅ |
| versions/*.py | 迁移版本文件 | ✅ |

## 四、前端文件 (frontend/)

### 4.1 构建产物 (frontend/dist/)
| 目录/文件 | 说明 | 必须 |
|-----------|------|------|
| index.html | 入口HTML | ✅ |
| assets/*.js | JS资源 | ✅ |
| assets/*.css | CSS资源 | ✅ |

### 4.2 源代码 (frontend/src/)
| 目录/文件 | 说明 | 必须 |
|-----------|------|------|
| main.js | 入口JS | ✅ |
| App.vue | 根组件 | ✅ |
| api/ | API调用 | ✅ |
| components/ | Vue组件 | ✅ |
| views/ | 页面视图 | ✅ |
| stores/ | Pinia状态 | ✅ |
| router/ | 路由配置 | ✅ |

### 4.3 配置文件
| 文件 | 说明 | 必须 |
|------|------|------|
| package.json | npm配置 | ✅ |
| vite.config.js | Vite配置 | ✅ |
| index.html | HTML模板 | ✅ |

## 五、Nginx配置 (nginx/)

| 文件 | 说明 | 必须 |
|------|------|------|
| nginx.conf | Nginx主配置 | ✅ |
| ssl-setup.sh | SSL证书脚本 | 可选 |

## 六、部署脚本 (scripts/)

| 文件 | 说明 | 必须 |
|------|------|------|
| deploy.sh | 部署脚本 | ✅ |
| deploy-cloud.sh | 云端部署脚本 | ✅ |
| download-model.sh | 模型下载脚本 | ✅ |

## 七、模型文件（需单独处理）

| 目录 | 说明 | 大小 |
|------|------|------|
| backend/data/chroma/models/models--sentence-transformers--all-MiniLM-L6-v2/ | 嵌入模型 | ~90MB |

**注意**: 模型文件太大，不上传GitHub。部署时需要：
1. 本地下载后上传到服务器
2. 或在服务器上运行 `bash scripts/download-model.sh`（需设置代理）

## 八、部署步骤

### 步骤1: 推送代码到GitHub
```bash
cd "项目目录"
git add .
git commit -m "feat: 云端部署配置"
git push origin main
```

### 步骤2: 服务器克隆项目
```bash
git clone https://github.com/你的用户名/creative-master-cloud.git
cd creative-master-cloud
```

### 步骤3: 配置环境变量
```bash
cp .env.cloud .env
# 编辑.env，设置SECRET_KEY
nano .env
```

### 步骤4: 下载嵌入模型（如需代理）
```bash
export HTTP_PROXY=http://代理地址:端口
export HTTPS_PROXY=http://代理地址:端口
bash scripts/download-model.sh
```

### 步骤5: 执行部署
```bash
bash scripts/deploy.sh
```

## 九、本地开发环境

本地开发使用非容器化环境：

```bash
# 安装依赖
run-local.bat install

# 启动开发环境（支持热更新）
run-local.bat start

# 停止服务
run-local.bat stop

# 查看状态
run-local.bat status
```

访问地址：
- 前端开发服务器: http://localhost:5173
- 后端 API: http://localhost:7000
- API 文档: http://localhost:7000/docs

## 十、关键修复说明

本次部署已包含以下修复：

1. **requirements.txt**: 添加了 `pysqlite3-binary` 和 `PyJWT`
2. **vector_store.py**: 添加了 pysqlite3 替换 sqlite3 的代码
3. **docker-compose.prod.yml**: 移除了过时的 `version` 字段
4. **nginx.conf**: 配置为HTTP模式（无SSL）
