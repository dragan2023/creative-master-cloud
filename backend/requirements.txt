# ============================================
# 全能创意大师 - Python 依赖清单
# 测试通过：Python 3.10+ / Windows 11
# 镜像源：https://pypi.tuna.tsinghua.edu.cn/simple
# 可复现锁：搭配 constraints.txt 收紧核心库兼容上界
#   pip install -r requirements.txt -c constraints.txt
# ============================================

# FastAPI 核心框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0

# 数据库 (SQLite + PostgreSQL)
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.1
redis>=5.0.1

# 向量数据库
chromadb>=0.5.0
networkx>=3.0
pysqlite3-binary>=0.5.0
sentence-transformers>=2.2.0

# LangChain 核心
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0

# LLM API 客户端
openai>=1.10.0
dashscope>=1.14.1
google-genai>=1.0.0

# 日志
loguru>=0.7.2

# 安全与认证
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# 文档预处理
# pypdf用于PDF解析
pypdf>=4.0.0
python-docx>=1.1.0
# openpyxl用于Excel(.xlsx/.xls)解析
openpyxl>=3.1.0

# HTTP 客户端
requests>=2.31.0
urllib3>=1.26.0,<3.0.0
httpx>=0.26.0
aiohttp>=3.9.1
aiofiles>=23.0.0

# 配置与工具
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
readability-lxml>=0.8.1

# 数据处理
numpy>=1.24.0
pyyaml>=6.0

# 系统监控（monitoring 服务的 CPU/内存/磁盘指标）
psutil>=5.9.0

# JSON修复（处理LLM返回的异常JSON）
json-repair>=0.58.0

# 测试依赖
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ==================== 可选依赖 ====================
# torch - GPU加速支持（可选，用于深度学习相关功能）
# 安装GPU版本: pip install torch --index-url https://download.pytorch.org/whl/cu126
# 安装CPU版本: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0
