"""
提示词优化器
将用户笼统的创意描述优化为结构化的提示词
支持短视频脚本、剧本大纲、小说大纲、平面广告、TVC广告等板块

@date: 2026-04-02
@version: v3.0.0
@author: 周金磊
@contact: QQ：7527149（添加时请说明来意）
"""
from typing import Optional, Dict, Any
from sqlalchemy.ext.asyncio import AsyncSession

from app.agents.llm_manager import get_llm_manager
from app.core.logger import get_logger


# ==================== 优化提示词模板 ====================

OPTIMIZER_PROMPTS = {
    "short_video": {
        "name": "短视频脚本优化",
        "description": "将笼统的短视频描述优化为结构化脚本需求",
        "content": """你是一位专业的短视频脚本策划专家，擅长将用户的创意描述转化为结构化的脚本需求。

## ⚠️ 核心原则
**你必须严格围绕用户的原始描述进行优化，不得遗漏或偏离用户提供的任何关键信息！**
- 用户原始描述中的所有具体细节、关键词、数字、名称都必须在优化结果中体现
- 如果用户已明确指定了某些要素（如平台、时长、风格等），必须原样保留，不得随意修改
- 优化是帮助结构化和补充，而不是重写或替代用户的创意

## 原始描述（用户输入）
{original_text}

## 分析步骤（请先分析原始描述）
1. 【提取关键词】从原始描述中提取所有关键词和核心概念
2. 【识别具体要素】识别用户已明确指定的：平台、时长、风格、受众、场景等
3. 【定位模糊点】找出需要补充完善的部分

## 优化要求
1. **核心主题**：从原始描述中提炼，保留用户的具体表述
2. **平台适配**：如用户已指定平台，必须保留；如未指定，根据内容特点推荐
3. **受众定位**：基于原始描述推断，如用户已指定则原样保留
4. **场景元素**：从原始描述中提取具体场景，可适当扩展
5. **情感基调**：从原始描述中推断，保留用户的风格偏好
6. **网感元素**：根据平台特性补充适合的网感表达方式

## 输出格式

【核心主题】从原始描述提炼的主题（保留用户的具体表述）

【平台与时长】用户指定或推荐的平台及时长

【目标受众】基于原始描述的受众分析

【创意亮点】（从原始描述中提取，保留用户原意）
- 亮点1（来自原始描述）
- 亮点2（来自原始描述）
- 补充亮点（基于原始意图的合理延伸）

【关键元素】
- 场景：原始描述中的场景 + 补充
- 人物：原始描述中的人物 + 补充
- 道具/元素：原始描述中的元素 + 补充

【情感基调】从原始描述推断的基调

【网感建议】适合该平台的传播技巧

## 输出要求
- 总字数控制在350字以内
- 直接输出优化内容，不要开场白
- **所有来自原始描述的信息必须标注或体现**"""
    },

    "script": {
        "name": "剧本大纲优化",
        "description": "将故事概念转化为完整的剧本大纲需求",
        "content": """你是一位资深编剧，擅长将用户的剧本创意转化为结构化的大纲需求。

## ⚠️ 核心原则
**你必须严格围绕用户的原始描述进行优化，不得遗漏或偏离用户提供的任何关键信息！**
- 用户原始描述中的所有具体细节（剧名、人物名、情节、设定等）都必须保留
- 如果用户已明确指定了剧集类型、题材、投放平台、对标作品等，必须原样保留
- 优化是帮助结构化和完善，而不是重写用户的故事创意

## 原始描述（用户输入）
{original_text}

## 分析步骤（请先分析原始描述）
1. 【提取关键要素】剧名、人物、情节节点、世界观设定等
2. 【识别明确指定项】剧集类型、题材、平台、集数、对标作品等
3. 【定位需要补充的部分】未明确但故事需要的要素

## 优化要求
1. **剧集类型**：如用户已指定（院线电影/网络电影/长剧/短剧等），必须保留
2. **题材定位**：如用户已指定题材，必须保留；如未指定，根据故事推断
3. **投放平台**：如用户已指定平台，必须原样保留
4. **对标作品**：如用户提及对标作品，必须体现在优化结果中
5. **故事梗概**：保留用户原始描述中的所有情节要点，按结构重组
6. **人物设定**：保留用户提供的所有人物信息，可补充完善

## 输出格式

【剧集类型】用户指定或推荐的类型

【题材定位】用户指定或推断的题材

【投放平台】用户指定的平台（如有）

【对标作品】用户提及的对标作品（如有）

【一句话梗概】20字以内的故事核心（基于原始描述）

【核心冲突】从原始描述中提炼的主要矛盾

【主题立意】从原始描述中提炼的核心思想

【主要人物】（保留原始描述中的所有人物）
- 主角：原始描述中的主角设定
- 对手/反派：原始描述中的冲突来源
- 配角：原始描述中的其他关键角色

【故事脉络】（基于原始描述的情节重组）
- 开端：原始描述中的起始情节
- 发展：原始描述中的情节推进
- 高潮：原始描述中的冲突爆发
- 结局：原始描述中的收束方式

【独特卖点】基于原始描述的独特之处

## 输出要求
- 总字数控制在450字以内
- 直接输出优化内容，不要开场白
- **所有来自原始描述的具体信息必须保留**"""
    },

    "novel": {
        "name": "小说大纲优化",
        "description": "将故事概念转化为完整的小说大纲需求",
        "content": """你是一位资深网文策划和文学编辑，擅长将用户的小说创意转化为具有商业潜力的大纲需求。

## ⚠️ 核心原则
**你必须严格围绕用户的原始描述进行优化，不得遗漏或偏离用户提供的任何关键信息！**
- 用户原始描述中的所有具体细节（书名、人物名、情节、设定等）都必须保留
- 如果用户已明确指定了篇幅、类型、平台、基调等，必须原样保留
- 优化是帮助结构化和完善，而不是重写用户的故事创意

## 原始描述（用户输入）
{original_text}

## 分析步骤（请先分析原始描述）
1. 【提取关键要素】书名、主角、核心情节、世界观设定等
2. 【识别明确指定项】篇幅、类型标签、目标平台、基调氛围等
3. 【定位需要补充的部分】未明确但小说需要的要素

## 优化要求
1. **篇幅定位**：如用户已指定（短篇/中篇/长篇），必须保留
2. **类型标签**：如用户已指定类型，必须保留；如未指定，根据内容推断
3. **目标平台**：如用户已指定平台（起点/晋江/番茄等），必须原样保留
4. **基调氛围**：如用户已指定基调（正剧/喜剧/虐恋/爽文等），必须保留
5. **故事梗概**：保留用户原始描述中的所有情节要点
6. **人物设定**：保留用户提供的所有人物信息

## 输出格式

【篇幅定位】用户指定或推荐的篇幅

【类型标签】用户指定或推断的类型

【目标平台】用户指定的平台（如有）

【基调氛围】用户指定的基调（如有）

【一句话梗概】20字以内的故事核心（基于原始描述，要够吸引人）

【核心矛盾】从原始描述中提炼的主要困境

【爽点/泪点设计】（基于原始描述分析）
- 核心爽点：从原始描述中提取的爽感来源
- 情感触动：从原始描述中提取的情感线

【主角人设】（保留原始描述中的所有信息）
- 身份定位：原始描述中的身份设定
- 核心特质：原始描述中的性格特点
- 成长路径：原始描述中的成长变化

【世界观亮点】从原始描述中提取的独特设定

【故事钩子】基于原始描述的开篇吸引点

【独特卖点】基于原始描述的核心竞争力

## 输出要求
- 总字数控制在450字以内
- 直接输出优化内容，不要开场白
- **所有来自原始描述的具体信息必须保留**"""
    },

    "print_ad": {
        "name": "平面广告创意优化",
        "description": "将模糊的广告需求转化为精准的创意简报",
        "content": """你是一位国际4A广告公司的创意总监，擅长将用户的广告需求转化为精准的创意简报。

## ⚠️ 核心原则
**你必须严格围绕用户的原始描述进行优化，不得遗漏或偏离用户提供的任何关键信息！**
- 用户原始描述中的所有具体细节（品牌名、产品名、卖点、受众等）都必须保留
- 如果用户已明确指定了品牌产品、广告目的、核心信息、受众特征等，必须原样保留
- 优化是帮助结构化和完善，而不是替代用户的创意想法

## 原始描述（用户输入）
{original_text}

## 分析步骤（请先分析原始描述）
1. 【提取品牌产品】品牌名称、产品名称、产品特点
2. 【识别明确指定项】广告目的、核心信息、受众特征、风格调性等
3. 【定位需要补充的部分】未明确但广告需要的要素

## 优化要求
1. **品牌产品**：必须保留用户提供的品牌名和产品名（如有）
2. **广告目的**：如用户已指定目的，必须原样保留
3. **核心信息**：用户提供的核心信息是创意的灵魂，必须完整保留
4. **受众特征**：保留用户提供的所有受众细节，可补充完善
5. **风格调性**：如用户已指定风格，必须保留
6. **尺寸媒介**：如用户提及尺寸或发布媒介，必须保留

## 输出格式

【品牌产品】用户提供的品牌和产品信息

【广告目的】用户指定的广告目的

【核心卖点】从原始描述中提炼的核心优势

【核心信息】用户希望传达的核心信息（必须保留原意）

【目标受众】（保留原始描述中的所有受众信息）
- 人群画像：原始描述中的年龄、性别、职业等
- 核心痛点：从原始描述推断
- 心理需求：从原始描述推断

【接触场景】用户提及或推断的接触场景

【风格调性】用户指定的风格（如有）

【创意方向】
- 视觉风格：基于原始描述的建议
- 情感连接：如何打动受众
- 记忆钩子：让人记住的关键元素

【行动召唤】用户提及或推荐的行动引导

【差异化】基于原始描述的独特之处

## 输出要求
- 总字数控制在350字以内
- 直接输出优化内容，不要开场白
- **所有来自原始描述的具体信息必须保留**"""
    },

    "tvc": {
        "name": "TVC广告脚本优化",
        "description": "将模糊的TVC需求转化为完整的创意脚本需求",
        "content": """你是一位国际4A广告公司的创意总监，擅长将用户的TVC广告需求转化为完整的创意脚本需求。

## ⚠️ 核心原则
**你必须严格围绕用户的原始描述进行优化，不得遗漏或偏离用户提供的任何关键信息！**
- 用户原始描述中的所有具体细节（品牌名、产品名、时长、平台等）都必须保留
- 如果用户已明确指定了品牌产品、广告目的、核心信息、时长等，必须原样保留
- 优化是帮助结构化和完善，而不是替代用户的创意想法

## 原始描述（用户输入）
{original_text}

## 分析步骤（请先分析原始描述）
1. 【提取品牌产品】品牌名称、产品名称、产品特点
2. 【识别明确指定项】广告目的、核心信息、时长、投放平台、风格调性等
3. 【定位需要补充的部分】未明确但TVC需要的要素

## 优化要求
1. **品牌产品**：必须保留用户提供的品牌名和产品名
2. **广告目的**：如用户已指定目的，必须原样保留
3. **核心信息**：用户提供的核心信息必须完整保留
4. **时长要求**：如用户指定时长（15秒/30秒/60秒等），必须保留
5. **投放平台**：如用户提及平台，必须保留
6. **风格调性**：如用户已指定风格，必须保留

## 输出格式

【品牌产品】用户提供的品牌和产品信息

【广告目的】用户指定的广告目的

【时长要求】用户指定的时长（如有）

【投放平台】用户指定的平台（如有）

【品牌传播核心】
- 核心卖点：从原始描述提炼的产品优势
- 品牌调性：用户指定或推断的品牌形象
- 一句话主张：观众应该记住什么（基于原始描述）

【目标受众】（保留原始描述中的所有受众信息）
- 人群画像：原始描述中的年龄、性别、职业等
- 媒介习惯：推断的观看场景
- 情感触发点：什么能打动他们

【创意概念】
- 核心创意：基于原始描述的创意主线
- 故事梗概：一句话概括广告故事（基于原始描述）
- 情感曲线：广告的情感走向

【记忆点设计】
- 视觉符号：能被记住的画面元素
- 听觉符号：音乐/音效/台词记忆点
- 品牌露出：如何自然植入品牌

【创意亮点】基于原始描述的独特之处

## 输出要求
- 总字数控制在400字以内
- 直接输出优化内容，不要开场白
- **所有来自原始描述的具体信息必须保留**"""
    },

    "original_ip": {
        "name": "原创IP计划优化",
        "description": "将模糊的IP角色概念转化为完整的角色档案需求",
        "content": """你是一位资深的IP角色设计专家，擅长将用户的IP角色创意转化为具有商业价值和情感吸引力的角色档案需求。

## ⚠️ 核心原则
**你必须严格围绕用户的原始描述进行优化，不得遗漏或偏离用户提供的任何关键信息！**
- 用户原始描述中的所有具体细节（角色名、外观、性格、背景等）都必须保留
- 如果用户已明确指定了目标平台、参考IP、商业目标等，必须原样保留
- 优化是帮助结构化和完善，而不是重新设计用户的角色

## 原始描述（用户输入）
{original_text}

## 分析步骤（请先分析原始描述）
1. 【提取角色要素】角色名、外观特征、性格特点、能力/技能等
2. 【识别明确指定项】目标平台、参考IP、商业目标、特殊要求等
3. 【定位需要补充的部分】未明确但角色档案需要的要素

## 优化要求
1. **角色名称**：如用户已提供角色名，必须原样保留
2. **外观特征**：保留用户描述的所有外观细节，可补充完善
3. **性格特点**：保留用户描述的性格关键词，可展开行为表现
4. **背景故事**：如用户提供背景，必须保留核心设定
5. **目标平台**：如用户指定平台（漫画/动画/游戏等），必须保留
6. **参考IP**：如用户提及参考IP，必须体现在优化结果中

## 输出格式

【角色名称】用户提供的角色名（或基于描述的建议名称）

【目标平台】用户指定的平台（如有）

【参考IP】用户提及的参考IP（如有）

【核心记忆锚点】（保留原始描述中的所有特征）
- 视觉特征：原始描述中的外观特点 + 补充
- 性格标签：原始描述中的性格关键词 + 行为表现
- 标志性动作/口头禅：原始描述中的标识 + 补充

【背景故事】（保留原始描述中的核心设定）
- 起源：原始描述中的诞生/来源
- 存在意义：原始描述中的驱动力
- 现状：原始描述中的当前状态

【语言与行为】
- 说话方式：基于性格推断的语气、用词
- 行为模式：基于性格的习惯、技能、弱点

【关系网络】
- 重要的人：原始描述中提及的关系
- 潜在对手/伙伴：可发展的故事冲突

【商业潜力】
- 目标受众：谁会被这个角色吸引
- 衍生方向：适合的开发方向
- 情感共鸣点：观众能产生什么情感连接

## 输出要求
- 总字数控制在450字以内
- 直接输出优化内容，不要开场白
- **所有来自原始描述的具体信息必须保留**"""
    }
}


class PromptOptimizer:
    """提示词优化器"""

    def __init__(self):
        self.logger = get_logger("prompt_optimizer")
        self.llm_manager = get_llm_manager()

    def get_supported_modules(self) -> Dict[str, str]:
        """获取支持的模块列表"""
        return {
            module: info["name"]
            for module, info in OPTIMIZER_PROMPTS.items()
        }

    async def optimize(
        self,
        db: AsyncSession,
        user_id: int,
        module: str,
        original_text: str
    ) -> Dict[str, Any]:
        """
        执行提示词优化

        Args:
            db: 数据库会话
            user_id: 用户ID
            module: 模块名称（short_video/script/novel/print_ad/tvc）
            original_text: 原始描述文本

        Returns:
            优化结果字典，包含:
            - optimized_text: 优化后的文本
            - original_length: 原始文本长度
            - optimized_length: 优化后文本长度
        """
        # 验证模块
        if module not in OPTIMIZER_PROMPTS:
            raise ValueError(
                f"不支持的模块: {module}，支持的模块: {list(OPTIMIZER_PROMPTS.keys())}")

        # 验证输入
        if not original_text or len(original_text.strip()) < 5:
            raise ValueError("原始描述太短，请至少输入5个字符")

        # 获取优化提示词模板
        template = OPTIMIZER_PROMPTS[module]
        prompt = template["content"].format(
            original_text=original_text.strip())

        self.logger.info(
            f"开始优化提示词 - 模块: {module}, 用户: {user_id}, 原文长度: {len(original_text)}")

        try:
            # 获取LLM提供者（使用用户默认配置或系统配置）
            provider = await self.llm_manager.get_provider_from_db(db, user_id)

            # 调用LLM进行优化
            response = await provider.generate(prompt)

            optimized_text = response.content.strip()

            self.logger.info(f"优化完成 - 优化后长度: {len(optimized_text)}")

            return {
                "optimized_text": optimized_text,
                "original_length": len(original_text),
                "optimized_length": len(optimized_text),
                "module": module,
                "module_name": template["name"]
            }

        except Exception as e:
            self.logger.error(f"优化失败: {str(e)}")
            raise Exception(f"提示词优化失败: {str(e)}")


# 全局优化器实例
prompt_optimizer = PromptOptimizer()


def get_prompt_optimizer() -> PromptOptimizer:
    """获取提示词优化器实例"""
    return prompt_optimizer
