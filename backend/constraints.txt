# ============================================
# 全能创意大师 - 依赖约束文件（可审计的可复现锁）
# ============================================
# 作用：为核心直接依赖锁定"经过验证的兼容上界"，防止 pip 解析出
#       未经迁移验证的破坏性大版本（例如 pydantic 3.x、sqlalchemy 2.1+）。
#
# 使用方式（与 requirements.txt 配合，约束不新增依赖，仅收紧版本上界）：
#   pip install -r requirements.txt -c constraints.txt
#
# 维护规则：
#   - 升级任一核心库大版本前，必须先在此文件放宽上界并完成迁移验证；
#   - 严禁以放宽约束的方式绕过 preflight-release.ps1 的门禁失败。
# --------------------------------------------

# Web 框架与配置（Pydantic v2 系列已验证）
fastapi<1.0.0
pydantic>=2.5.3,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
uvicorn<1.0.0
python-multipart>=0.0.6,<1.0.0
email-validator>=2.1.0,<3.0.0

# 数据库与迁移（SQLAlchemy 2.0 系列已验证）
sqlalchemy>=2.0.25,<2.1.0
alembic<2.0.0
asyncpg<1.0.0
aiosqlite<1.0.0
psycopg2-binary>=2.9.0,<3.0.0
redis>=5.0.1,<8.0.0

# 向量库与图谱
chromadb>=0.5.0,<1.0.0
networkx<4.0.0
sentence-transformers<4.0.0

# 平台兼容例外：pysqlite3-binary 是 Chroma 的可选 SQLite 兼容层。
# Windows + Python 3.13 无稳定预编译包；应用会回退到标准库 sqlite3，
# 因此不在此文件强制解析或安装。Linux 生产镜像需单独验证该可选包。

# LangChain 生态（0.2/0.3 系列已验证）
langchain>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
langchain-openai>=0.1.0,<0.3.0
langchain-google-genai>=1.0.0,<3.0.0

# LLM API 客户端
openai>=1.10.0,<2.0.0
dashscope>=1.14.1,<2.0.0
google-genai>=1.0.0,<2.0.0

# 数据处理
numpy>=1.24.0,<3.0.0
pyyaml>=6.0,<7.0.0

# 文档、网络与配置工具
pypdf>=4.0.0,<7.0.0
python-docx>=1.1.0,<2.0.0
openpyxl>=3.1.0,<4.0.0
requests>=2.31.0,<3.0.0
urllib3>=1.26.0,<3.0.0
httpx>=0.26.0,<1.0.0
aiohttp>=3.9.1,<4.0.0
aiofiles>=23.0.0,<26.0.0
python-dotenv>=1.0.0,<2.0.0
json-repair>=0.58.0,<1.0.0

# 安全与日志
loguru>=0.7.2,<1.0.0
pyjwt>=2.8.0,<3.0.0
bcrypt==4.0.1

# 系统监控
psutil>=5.9.0,<7.0.0
